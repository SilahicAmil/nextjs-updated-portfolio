const ProjectTags = ({ children }) => {
  return (
    <span class=" m-2  inline-block  gap-4 rounded-full bg-gray-200 px-2  text-sm font-semibold text-gray-700">
      {children}
    </span>
  );
};

export default ProjectTags;
